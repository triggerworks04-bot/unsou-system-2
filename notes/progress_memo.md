# 進捗メモ（unsou-system-2）

手軽な作業ログ。設計変更の要点は必ず `docs/` と `schema_master.md` に反映すること。

## 2026-04-30

- GitHub管理用ドキュメント初版を作成（README, docs/*, 本メモ）。
- 方針: スプレッドシートDB + AppSheet、配車上流、運行予定ID＋共通マスタID連携。

## 2026-04-30（追記）：Claude Code 初回レビュー反映

- **実施**：Claude Code 初回レビュー結果を設計ドキュメントへ反映した。
- **採用した修正内容の要約**：
  - **ID／採番**：内部Keyは **AppSheet UNIQUEID()**、人向けは **表示用番号を別列**（初期は内部Key必須、表示用番号は後続）。
  - **schema_master**：共通ルールに ID・採番を追記。全テーブルに **AppSheet列種別**・**参照先（Ref）** を付与。
  - **配車予定**：ステータス、カレンダーイベントID、最終同期日時、LINE送信済みフラグ、出発地／目的地 を追加。**予定変更は同一運行予定ID維持＋ステータス・更新日時** と明記。
  - **点呼**：点呼種別、アルコール検知値、異常有無、体調確認区分、指示事項、点呼者ID を追加。
  - **運行日報**：**ヘッダ**と**運行明細**に分割。実出発・実到着・便番号・出発地・目的地は **明細側** に配置。
  - **売上**：運行日報ID Ref、請求書番号、入金確認、入金日、請求月 を追加。
  - **LINE通知**：`docs/line_notification_design.md` を新規追加。キュー列と重複防止・GAS でのフラグ更新方針を記載。
- **決定**：採番方式は **内部Key＝UNIQUEID()**、**表示用番号＝別列** で確定。
- **次の作業**：配車予定表の実データ確認に基づき **`schema_master.md` の精密化**（列名確定・必須条件の確定）。

## 2026-04-30（追記）：Claude Code 再レビュー反映

- **実施**：Claude Code **再レビュー** を実施し、軽微修正を反映した。
- **確認した修正（3系統）**：車両マスタ列名、`schema_master.md` と日報設計での運行予定IDの意図明確化、型の仮決定（請求月・予定時刻）。
- **車両マスタ**：**車両番号_表示用**（帳票・管理上の表示用番号）、**車両呼称**（現場のナンバー／社内呼称、AppSheet Label 候補）に整理。
- **日報**：ヘッダの運行予定IDは「代表予定」の示し／明細は「便ごとの対応予定」、複数予定時は実績・売上は**明細の運行予定ID優先**、AppSheet で明細へヘッダから初期値引き継ぎつつ変更可能、を明記。
- **請求月**：AppSheet **Date**・**月初日**管理に仮決定（例: 2026年4月分は `2026-04-01`）。
- **出発／到着予定時刻（配車）**：AppSheet **Time** に仮決定。運行日は **`予定日`**。カレンダー用 DateTime は GAS で合成。日跨ぎは後続で DateTime 拡張を検討。
- **次の作業**：**配車予定表_2026年マスター.xlsx** の実データ確認と、**10_配車予定**（配車シート／ブック内名称）の**列定義精密化**。

## 配車予定表の二層構造を採用

- **配車予定表_2026年マスター.xlsx** は、**印刷・入力・確認用**の**横持ち**月別表として残す（A3・掲示）。
- **システム連携**には、横持ち表から**変換**して作る **`10_配車予定`** を使う。
- **10_配車予定** は **「1日・1案件・1担当者・1車両 ＝ 1行」** の**縦持ちDB**とする。
- **Googleカレンダー**、**LINE通知**、**点呼記録簿**、**運行日報**、**売上管理表**は **10_配車予定** を起点に連携する。
- **次の作業**：**配車予定表_2026年マスター.xlsx** の**実際の列範囲・行構造**を確認し、**変換対象の列インデックスと固定セル位置**を `dispatch_conversion_design.md` と整合させて確定する。

## 配車予定表→10_配車予定 変換ルール設計書を追加

- **`docs/dispatch_conversion_design.md`** を新規作成した。
- 月別**横持ち**配車表から **10_配車予定** への変換ルールを整理した。
- **1日・1案件・1担当者・1車両 ＝ 1行**の縦持ちDB方針を明記した。
- **担当者行・車両行**の**2行セット**読み取りルールを整理した。
- **空欄セル**、**マスタ照合**、**重複防止**、**変更・キャンセル**、**下流連携**を整理した。
- **次の作業**：**配車予定表_2026年マスター.xlsx** の実際の**列範囲・行構造**を確認し、変換対象の**列範囲と固定セル位置**を確定する。

## 次のアクション（例）

- [ ] Google Drive 上の各資料の実パス／リンクを社内共有（本リポジトリには秘密情報を書かない）
- [ ] 配車シートの試作と AppSheet プロトタイプ
- [ ] **10_配車予定** 生成の**実装方針**（GAS／関数）を決定しプロトタイプ化（`dispatch_conversion_design.md` の確定レイアウトに準拠）

## 配車予定表の実レイアウト確認

- **配車予定表_2026年マスター.xlsx** の実データを確認した。
- **A列＝日付**、**B列＝曜日**、**C列＝区分**として扱う方針を確定した。
- **D列〜V列**を案件列として **`10_配車予定`** への変換対象とする。
- **W列「担当者」**と**X列「出勤日数」**は変換対象外とする。
- **C列「担当者」行**と直下の**「車両」行**を**2行セット**として読み取る。
- **次の作業**：本変換ルールに基づき、**GAS**または**スプレッドシート関数**で **`10_配車予定`** を生成する**実装方針**を決める。

## 10_配車予定・曜日列の追加（軽微修正）

- **10_配車予定**に**曜日**列を追加した（`schema_master.md` **B.**）。
- 曜日は**予定日から自動生成**する**表示用**項目とする。
- **LINE通知**・**カレンダー確認**・**一覧表示**で利用する。

## 配車予定表→10_配車予定 GAS実装仕様書を追加

- **`docs/dispatch_conversion_gas_spec.md`** を新規作成した。
- 初期実装は**手動実行**または**ボタン実行**とする。
- 月別**横持ち**配車表から **`10_配車予定`** を生成する **GAS 関数案**を整理した。
- 初期実装では**カレンダー同期**・**LINE通知**・**高度なマスタ照合**は行わない。
- **重複防止**は **作成元シート名・予定日・案件名_入力値・作成元セル**の複合キーで行う。
- **次の作業**：GAS 第1版の**実装指示**を作成し、Cursor で `scripts/dispatch_conversion.gs` などの**初期コード**を作ること。

## GAS第1版コード作成

- **`scripts/dispatch_conversion.gs`** を新規作成した。
- **スプレッドシートのメニュー「配車変換」→「現在のシートを10_配車予定へ変換」** で、現在のシートから **10_配車予定** へ書き込む方針とした。
- 初期実装では **A列＝日付**、**B列＝曜日**、**C列＝区分**、**D列〜V列＝案件列** を読み取る。
- **C列「担当者」行**と直下の**「車両」行**を**2行セット**で処理する。
- **10_配車予定** は**追加または更新（UPSERT）**で反映する。
- **カレンダー同期**・**LINE通知**・**高度なマスタ照合**は後続対応とする。
- **次の作業**：実際の Googleスプレッドシートに `scripts/dispatch_conversion.gs` を貼り付け、**テスト用シート**で変換テストを行うこと。

## 2026-04-30（追記）：10_配車予定への setValues 行数整合

- **修正**：`upsertScheduleRows_()` で、`getRange()` の行数と `setValues()` の二次元配列の行数が必ず一致するよう整理した（追記は `appendValues.length` 基準・無効／空の追記候補は除外）。
- **ログ**：追加対象行数・更新対象行数・実際に setValues する行数を `Logger.log` 出力した。

## メモ欄

（自由記述）
